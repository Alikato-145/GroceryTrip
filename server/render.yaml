services:
  - type: web
    name: grocerytrip-api
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: FRONTEND_URL
        sync: false   # ใส่เองใน Render dashboard
